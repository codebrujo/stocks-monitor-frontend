import {
    makeStyles
} from '@material-ui/core';

const portfolioSummaryStyles = makeStyles(() => ({
    root: {},
    portfoliosummary: {
        margin: '20px auto 20px auto',
    }
}));

export default portfolioSummaryStyles;
